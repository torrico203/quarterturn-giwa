{
  "name": "quarterturn-giwa",
  "version": "0.1.0",
  "description": "QUARTER TURN x GIWA — on-chain season badge (soulbound) contracts for the QUARTER TURN web game portal",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:giwa-sepolia": "hardhat run scripts/deploy.js --network giwaSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.0"
  }
}
